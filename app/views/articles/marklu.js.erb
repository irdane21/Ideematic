<% if @flux.errors.any? %>
  jQuery('#fluxes').html("<%= j render 'fluxes/show', flux: @flux %>");
<% else %>
  jQuery('#fluxes').append("<%= j render 'fluxes/show', flux: @flux %>");
  jQuery('#switchlu').html(<%= link_to "Marqué comme non Lu", articles_markpalu_path(id: article.id), :remote => true %>)
<% end %>
