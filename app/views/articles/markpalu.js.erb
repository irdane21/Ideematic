<% if @flux.errors.any? %>
  jQuery('#fluxes').html("<%= j render 'fluxes/show', flux: @flux %>");
<% else %>
  jQuery('#${@article.id}').append("<%= j render 'fluxes/show', flux: @flux %>");
  jQuery('#switchlu').html(<%= link_to "Marqué comme Lu", articles_marklu_path(id: article.id), :remote => true %>)
<% end %>
